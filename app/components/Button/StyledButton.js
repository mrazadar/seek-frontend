import styled from 'styled-components';

import LaddaButton from 'react-ladda';
import buttonStyles from './buttonStyles';


const StyledButton = styled(LaddaButton)`
  ${buttonStyles}
`;

export default StyledButton;
